"data.pkl" contains the user-item interactions data and the split data use for training, validation and testing. 

"friends.pkl" contains the social links among users, and it can be decoded into an adjacant matrix (size of [num_user, num_user]) with function load_friend() in the hisrec.py file

"use_profile.npy" contains the normalized user attributes of size [num_user, profile_size=231]

"neighbor.npy" of size [num_user,similar_neighbor=20] is an adjacent matrix containing a specified number of linked social neighbors for each user, the introduction of this file is to speed up the training of attentive representation learning. 
